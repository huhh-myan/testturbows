name: "Deploy embed script"

on:
  push:
    branches:
      - main
    paths:
      - "packages/embeds/core/**"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        run: echo "hi"